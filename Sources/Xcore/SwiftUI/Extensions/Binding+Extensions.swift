//
// Xcore
// Copyright © 2020 Xcore
// MIT license, see LICENSE file for details
//

import SwiftUI

/*
Remove as constant should handle this for most parts.
extension Binding {
    /// `get` only binding with `noop` for setter.
    static func get(_ block: @autoclosure @escaping @isolated(any) @Sendable () -> Value) -> Self {
        .init(get: block, set: { _ in })
    }
}
*/
