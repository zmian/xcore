# Roadmap

- Evaluate watchOS support separately from the supported iOS package and example.
- Evaluate whether Swift Sharing can replace Pond while preserving its keychain,
  composite-store, and Codable storage requirements.

DocC generation is already supported by `make build-docc`.
